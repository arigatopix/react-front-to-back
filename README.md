# GITHUB setting initial
`git init`

`touch .gitignore`

`git commit -m empty`

`git tag EMPTY`

## สร้าง project หรือ folder
`mkdir PROJECTNAME`

## สร้าง Branch
`git branch BRANCHNAME EMPTY`

`git checkout BRANCHNAME`

`git add .`